// real example of static in flutter
class AppColors {
  // we use static to make it static
  // we use final to make it constant
  //
  static final Color primaryColor = Color(0xFF000000);
  static final Color secondaryColor = Color(0xFF000000);
}

// this is a class
class Color {
  Color(int i);
}
